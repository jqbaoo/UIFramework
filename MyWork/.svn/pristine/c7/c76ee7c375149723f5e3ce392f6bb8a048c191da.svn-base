using UnityEngine;
using System.Collections;

public class Test : MonoBehaviour {

	// Use this for initialization
	void Start () {
     Transform result =  GameTool.FindTheChild(this.gameObject, "Capsule");
        if (result!=null)
        {
            GameDebuger.Log("查找到了"+result.gameObject.name);
        }
	}
	
	
}
